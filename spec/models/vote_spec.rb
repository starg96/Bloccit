descibe Vote do
	describe "validation" do
		describe "value validation" do
			it "only allows -1 or 1 as values" do
				expect( value validation ).to eq(1) || expect( value validation ).to eq(-1)
			end
		end
	end
end